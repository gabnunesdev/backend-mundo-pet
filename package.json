{
  "name": "mundopet-backend",
  "version": "1.0.0",
  "description": "Backend fake para o projeto Mundo Pet",
  "main": "server.json",
  "scripts": {
    "server": "json-server --watch server.json --host 0.0.0.0 --port $PORT"
  },
  "author": "Gabriel Nunes",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
